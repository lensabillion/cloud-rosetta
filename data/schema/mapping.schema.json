{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/OWNER/all-cloud/data/schema/mapping.schema.json",
  "title": "Cloud Rosetta mapping file",
  "type": "object",
  "required": [
    "domain",
    "rows"
  ],
  "additionalProperties": false,
  "properties": {
    "domain": {
      "type": "string",
      "description": "Domain slug. Must match the filename stem.",
      "pattern": "^[a-z0-9-]+$"
    },
    "title": {
      "type": "string"
    },
    "rows": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/row"
      }
    }
  },
  "$defs": {
    "provider": {
      "type": "object",
      "required": [
        "name"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "Current official product name. Null means this cloud has no equivalent."
        },
        "url": {
          "type": "string",
          "format": "uri"
        },
        "formerly": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Names this was previously sold under. Readers search for these because stale courses still teach them."
        },
        "note": {
          "type": "string"
        },
        "maturity": {
          "enum": [
            "current",
            "legacy",
            "superseded"
          ],
          "description": "Lifecycle state. 'legacy' means still sold but no longer the default choice. 'superseded' means a newer product has taken its place. Adopted from og-aws's product maturity table so readers do not study a dead product."
        }
      }
    },
    "row": {
      "type": "object",
      "required": [
        "id",
        "concept",
        "divergence",
        "bite",
        "aws",
        "azure",
        "gcp",
        "verified"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9]+(\\.[a-z0-9-]+)+$",
          "description": "Stable dotted identifier, e.g. net.firewall.instance. Never reuse or renumber."
        },
        "concept": {
          "type": "string",
          "description": "The vendor-neutral thing being compared. Never a product name."
        },
        "divergence": {
          "enum": [
            "exact",
            "partial",
            "none"
          ],
          "description": "exact: the mental model transfers. partial: it maps but behaves differently in a way that changes answers. none: no honest equivalent."
        },
        "bite": {
          "enum": [
            "serious",
            "regular",
            "tip"
          ],
          "description": "How badly the difference bites, graded by consequence, following the og-aws legend. 'serious' where risks or costs are significant: critical security risk, meaningful financial cost, or an architectural choice that is fundamentally difficult to correct. 'regular' where the consequence is something not working, breaking, or not scaling gracefully. 'tip' for an important or often overlooked detail with no failure attached. Independent of `divergence`, which measures how far the mapping transfers rather than what it costs you."
        },
        "aws": {
          "$ref": "#/$defs/provider"
        },
        "azure": {
          "$ref": "#/$defs/provider"
        },
        "gcp": {
          "$ref": "#/$defs/provider"
        },
        "breaks_when": {
          "type": "string",
          "minLength": 40,
          "description": "REQUIRED unless divergence is exact. What actually differs, concretely enough to change a decision. This field is the product."
        },
        "shared_trap": {
          "type": "string",
          "minLength": 40,
          "description": "A caveat that applies identically to all three clouds. Use this when the mapping is genuinely exact but there is still something people get wrong, so that breaks_when keeps its strict meaning of 'where the mapping lies'."
        },
        "exam_tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Exam codes where this shows up, e.g. SAA-C03, AZ-104, ACE."
        },
        "sources": {
          "type": "array",
          "items": {
            "type": "string",
            "format": "uri"
          },
          "description": "Documentation pages supporting the breaks_when claim, not marketing pages."
        },
        "verified": {
          "type": "string",
          "format": "date",
          "description": "ISO date this row was last checked against vendor documentation."
        }
      }
    }
  }
}
