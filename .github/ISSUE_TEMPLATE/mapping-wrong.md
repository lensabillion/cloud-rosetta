---
name: A mapping is wrong
about: A row states something that is not true, or grades a mapping incorrectly
labels: correction
---

**Row id**
For example `net.firewall.instance`. Find it in `data/mappings/`.

**What it currently says**

**What is actually true**

**Vendor page that shows it**
Documentation, not a marketing page.

**Is the divergence grade also wrong?**
Grades are `exact`, `partial`, or `none`.
